# Postgres (1 instance, schemas: auth/notification)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=coredb
POSTGRES_USER=core
POSTGRES_PASSWORD=corepass

# NATS JetStream
NATS_URL=nats://localhost:4222

# Redis
REDIS_URL=redis://localhost:6379

# Shared secret for encrypt/decrypt (auth-service & notification-service must use the same value)
ENCRYPT_KEY=your-secret-key-min-16-chars
